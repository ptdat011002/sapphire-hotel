import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['lh3.googleusercontent.com', 'images.unsplash.com','plus.unsplash.com','avatars.githubusercontent.com'],
  },
};

export default nextConfig;
